#include <stdio.h>
int main(){
    int a,b,c,result;
    //ptintf("enter the value of a and b");
    scanf("%d %d %d",&a,&b,&c);
    result=a*b*c;
    printf("the multiplication result of given digits = %d",result);
}

