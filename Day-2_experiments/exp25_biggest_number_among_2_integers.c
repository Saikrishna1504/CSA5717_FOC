#include <stdio.h>
int main() {
    int a, b;
    printf("Please Enter Two different values\n");
    scanf("%d %d", &a, &b);
    if(a > b)
    {
        printf("a=%d is Largest", a);
    }
        printf("b=%d is Largest", b);  x
        return 0;
    }
